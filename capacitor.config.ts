import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ph.connect',
  appName: 'Punta Hermosa Connect',
  webDir: 'dist/public'
};

export default config;
